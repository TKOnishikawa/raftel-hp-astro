---
import BaseLayout from '../../layouts/BaseLayout.astro'
import Navigation from '../../components/Navigation.astro'

const contents = [
  {
    title: 'AI×プロンプトだけでプロ品質のHPを作る完全ガイド',
    description: 'コーディング知識ゼロから、AIプロンプトだけでプロ品質のホームページを制作したプロセスを全公開。',
    href: '/content/ai-hp-guide',
    date: '2026-02-12'
  },
  {
    title: '1記事から5チャネルへ — AI×SNS集客の全体設計',
    description: 'HP記事を起点に、X・YouTube・Instagram・LinkedInへ自動展開するAI活用のコンテンツ戦略。起業プロセスそのものをコンテンツにする実践ガイド。',
    href: '/content/ai-sns-strategy',
    date: '2026-02-12'
  },
  {
    title: '起業SNS設計の全体像 — 7つの設計原則×実践ケーススタディ',
    description: '起業時のSNS設計を7つの原則で解説。ターゲットの不安マッピングから、チャネル役割分担、プロフィール設計、導線設計まで。RAFTELの実例を交えて全公開。',
    href: '/content/sns-account-design',
    date: '2026-02-12'
  }
]
---

<BaseLayout
  title="Content | RAFTEL Inc."
  description="AI活用・データドリブン経営・DX実践ナレッジを発信。起業・新規事業に役立つ実践的な情報をお届けします。"
  ogImage="/images/og-content.jpg"
  currentPage="content"
>
  <Navigation currentPage="content" />

  <!-- Hero -->
  <header class="page-hero">
    <div class="page-hero-inner">
      <h1>Content</h1>
      <p>AI活用・データドリブン経営・DX実践ナレッジ</p>
    </div>
  </header>

  <!-- Breadcrumb -->
  <div class="breadcrumb">
    <a href="/">RAFTEL</a>
    <span>/</span>
    <span>Content</span>
  </div>

  <!-- Content List -->
  <main class="content-body">
    <div class="content-list">
      {contents.map(content => (
        <a href={content.href} class="content-item">
          <time class="content-date">{content.date}</time>
          <h2 class="content-title">{content.title}</h2>
          <p class="content-desc">{content.description}</p>
        </a>
      ))}
    </div>
  </main>
</BaseLayout>

<style>
.page-hero {
  padding: 140px 40px 80px;
  background: var(--bg-dark);
  color: var(--text-on-dark);
}

.page-hero-inner {
  max-width: var(--max-width);
  margin: 0 auto;
}

.page-hero h1 {
  font-family: var(--font-head);
  font-size: clamp(2rem, 5vw, 3rem);
  font-weight: 800;
  margin-bottom: 16px;
}

.breadcrumb {
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 20px 40px;
  font-size: 0.78rem;
  color: var(--text-muted);
  font-family: var(--font-en);
  display: flex;
  align-items: center;
  gap: 8px;
}

.breadcrumb a {
  transition: color 0.2s;
}

.breadcrumb a:hover {
  color: var(--accent);
}

.content-body {
  max-width: var(--max-width);
  margin: 0 auto;
  padding: 40px 40px 120px;
}

.content-list {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.content-item {
  padding: 32px;
  background: #fff;
  border: 1px solid var(--border);
  border-radius: 12px;
  transition: all 0.3s;
  display: block;
}

.content-item:hover {
  border-color: var(--accent);
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}

.content-date {
  font-size: 0.85rem;
  color: var(--text-muted);
  font-family: var(--font-en);
  display: block;
  margin-bottom: 12px;
}

.content-title {
  font-size: 1.5rem;
  font-weight: 700;
  margin-bottom: 12px;
  color: var(--text-primary);
}

.content-desc {
  color: var(--text-secondary);
  line-height: 1.8;
}

@media (max-width: 768px) {
  .page-hero {
    padding: 120px 24px 60px;
  }

  .breadcrumb, .content-body {
    padding-left: 24px;
    padding-right: 24px;
  }
}
</style>
